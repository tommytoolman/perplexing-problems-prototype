(function registerMeteorSirenBayesPage(){
"use strict";
window.poveyProblemPages=window.poveyProblemPages||{};

const PROBLEM="15.2";
const BASIS=10000;
const QUESTION=Object.freeze({priorPercent:1,sensitivityPercent:90,falsePositivePercent:5});
const hints=Object.freeze([
  "Separate the two ways the siren can sound: a true alarm on a meteor night, or a false alarm on an ordinary night.",
  "The true-alarm probability is P(M∩A)=P(M)P(A|M)=0.01×0.90.",
  "The false-alarm probability is P(not M∩A)=0.99×0.05.",
  "Conditioning on an alarm means dividing the true-alarm branch by the sum of both alarm branches.",
  "So P(M|A)=0.009/(0.009+0.0495)=0.153846… ."
]);
const resetMarkup='<button class="ghost-button" type="button" data-problem-action="p152-reset">Reset</button>';
const initialState=()=>({priorPercent:QUESTION.priorPercent,sensitivityPercent:QUESTION.sensitivityPercent,falsePositivePercent:QUESTION.falsePositivePercent,answer:"",feedback:"",feedbackTone:"neutral",committed:false,hintsUsed:0,revealed:false});
let state=initialState();

function clamp(value,min,max){return Math.max(min,Math.min(max,Number(value)));}
function format(value,digits=3){if(!Number.isFinite(value))return"—";const rounded=Number(value.toFixed(digits));return Object.is(rounded,-0)?"0":String(rounded);}
function escapeAttribute(value){return String(value).replaceAll("&","&amp;").replaceAll('"',"&quot;").replaceAll("<","&lt;").replaceAll(">","&gt;");}
function bayesValues(priorPercent=state.priorPercent,sensitivityPercent=state.sensitivityPercent,falsePositivePercent=state.falsePositivePercent){
  const prior=priorPercent/100,sensitivity=sensitivityPercent/100,falsePositive=falsePositivePercent/100,meteorAndAlarm=prior*sensitivity,noMeteorAndAlarm=(1-prior)*falsePositive,alarmProbability=meteorAndAlarm+noMeteorAndAlarm,posterior=alarmProbability===0?0:meteorAndAlarm/alarmProbability,meteorCount=BASIS*prior,noMeteorCount=BASIS*(1-prior),trueAlarmCount=BASIS*meteorAndAlarm,missedMeteorCount=BASIS*prior*(1-sensitivity),falseAlarmCount=BASIS*noMeteorAndAlarm,correctSilenceCount=BASIS*(1-prior)*(1-falsePositive),alarmCount=trueAlarmCount+falseAlarmCount;
  return{prior,sensitivity,falsePositive,meteorAndAlarm,noMeteorAndAlarm,alarmProbability,posterior,posteriorPercent:posterior*100,meteorCount,noMeteorCount,trueAlarmCount,missedMeteorCount,falseAlarmCount,correctSilenceCount,alarmCount,populationResidual:meteorCount+noMeteorCount-BASIS,branchResidual:trueAlarmCount+missedMeteorCount+falseAlarmCount+correctSilenceCount-BASIS,alarmResidual:alarmCount-trueAlarmCount-falseAlarmCount};
}
const challenge=bayesValues(QUESTION.priorPercent,QUESTION.sensitivityPercent,QUESTION.falsePositivePercent);
function parsePercent(raw){const normalized=String(raw).trim().replace(",",".");const match=normalized.match(/[-+]?\d*\.?\d+(?:e[-+]?\d+)?/i);if(!match)return NaN;const value=Number(match[0]);return !normalized.includes("%")&&value>0&&value<1?value*100:value;}

function probabilityVisual(){
  const values=bayesValues(),populationX=476,populationWidth=272,meteorWidth=populationWidth*values.prior,alarmTrueWidth=populationWidth*values.posterior;
  const terminal=[
    {y:73,label:"TRUE ALARM",count:values.trueAlarmCount,kind:"true"},
    {y:153,label:"MISSED METEOR",count:values.missedMeteorCount,kind:"miss"},
    {y:263,label:"FALSE ALARM",count:values.falseAlarmCount,kind:"false"},
    {y:343,label:"CORRECT SILENCE",count:values.correctSilenceCount,kind:"quiet"}
  ];
  return `<div class="p152-visual-wrap"><svg class="p152-visual" viewBox="0 0 780 430" role="img" aria-labelledby="p152-visual-title p152-visual-desc"><title id="p152-visual-title">Meteor-alarm probability tree and frequency mosaic</title><desc id="p152-visual-desc">With meteor prior ${format(state.priorPercent,2)} percent, sensitivity ${format(state.sensitivityPercent,2)} percent and false-positive rate ${format(state.falsePositivePercent,2)} percent, a population of ${BASIS} nights produces ${format(values.trueAlarmCount,2)} true alarms and ${format(values.falseAlarmCount,2)} false alarms. Among all ${format(values.alarmCount,2)} alarms, ${format(values.posteriorPercent,4)} percent correspond to meteors.</desc><text class="p152-panel-title" x="24" y="27">PROBABILITY TREE · EXPECTED FREQUENCIES OUT OF ${BASIS}</text><circle class="p152-root" cx="45" cy="211" r="8"/><text class="p152-root-label" x="45" y="233" text-anchor="middle">night</text><line class="p152-tree-line is-meteor" x1="53" y1="207" x2="153" y2="115"/><line class="p152-tree-line is-ordinary" x1="53" y1="215" x2="153" y2="305"/><text class="p152-edge-label is-meteor" x="91" y="143">meteor ${format(state.priorPercent,2)}%</text><text class="p152-edge-label" x="84" y="290">ordinary ${format(100-state.priorPercent,2)}%</text><circle class="p152-node is-meteor" cx="161" cy="108" r="9"/><circle class="p152-node is-ordinary" cx="161" cy="312" r="9"/><text class="p152-node-label is-meteor" x="177" y="111">M</text><text class="p152-node-label" x="177" y="315">not M</text><line class="p152-tree-line is-true" x1="170" y1="104" x2="315" y2="75"/><line class="p152-tree-line is-miss" x1="170" y1="113" x2="315" y2="155"/><line class="p152-tree-line is-false" x1="170" y1="307" x2="315" y2="265"/><line class="p152-tree-line is-quiet" x1="170" y1="316" x2="315" y2="345"/><text class="p152-edge-label is-true" x="211" y="75">alarm ${format(state.sensitivityPercent,2)}%</text><text class="p152-edge-label" x="211" y="151">silent ${format(100-state.sensitivityPercent,2)}%</text><text class="p152-edge-label is-false" x="205" y="267">alarm ${format(state.falsePositivePercent,2)}%</text><text class="p152-edge-label" x="205" y="348">silent ${format(100-state.falsePositivePercent,2)}%</text>${terminal.map((item)=>`<g class="p152-terminal is-${item.kind}"><rect x="315" y="${item.y-23}" width="114" height="48" rx="9"/><text class="p152-terminal-label" x="326" y="${item.y-5}">${item.label}</text><text class="p152-terminal-value" x="326" y="${item.y+15}">${format(item.count,2)} nights</text></g>`).join("")}<line class="p152-divider" x1="449" y1="45" x2="449" y2="398"/><text class="p152-panel-title" x="476" y="59">FREQUENCY MOSAIC</text><text class="p152-mosaic-kicker" x="476" y="87">ALL ${BASIS} NIGHTS</text><rect class="p152-mosaic-base" x="476" y="99" width="272" height="54" rx="8"/><rect class="p152-mosaic-segment is-meteor" x="476" y="99" width="${format(Math.max(meteorWidth,0),3)}" height="54" rx="8"/><line class="p152-meteor-pointer" x1="${format(populationX+meteorWidth,3)}" y1="99" x2="${format(populationX+Math.max(meteorWidth,24),3)}" y2="78"/><text class="p152-mosaic-label is-meteor" x="${format(populationX+Math.max(meteorWidth,28),3)}" y="75">${format(values.meteorCount,2)} meteor</text><text class="p152-mosaic-label" x="739" y="132" text-anchor="end">${format(values.noMeteorCount,2)} ordinary</text><text class="p152-mosaic-kicker" x="476" y="194">ONLY THE ${format(values.alarmCount,2)} ALARMS</text><rect class="p152-mosaic-base" x="476" y="206" width="272" height="75" rx="10"/><rect class="p152-mosaic-segment is-true" x="476" y="206" width="${format(Math.max(alarmTrueWidth,0),3)}" height="75" rx="10"/><text class="p152-alarm-label is-true" x="${format(476+Math.max(alarmTrueWidth/2,16),3)}" y="237" text-anchor="middle">true</text><text class="p152-alarm-value is-true" x="${format(476+Math.max(alarmTrueWidth/2,16),3)}" y="258" text-anchor="middle">${format(values.trueAlarmCount,2)}</text><text class="p152-alarm-label is-false" x="${format(476+alarmTrueWidth+(populationWidth-alarmTrueWidth)/2,3)}" y="237" text-anchor="middle">false</text><text class="p152-alarm-value is-false" x="${format(476+alarmTrueWidth+(populationWidth-alarmTrueWidth)/2,3)}" y="258" text-anchor="middle">${format(values.falseAlarmCount,2)}</text><rect class="p152-posterior-box" x="476" y="304" width="272" height="76" rx="12"/><text class="p152-posterior-kicker" x="493" y="328">METEOR GIVEN THAT THE SIREN SOUNDS</text><text class="p152-posterior-value" x="493" y="359">${format(values.posteriorPercent,4)}%</text><text class="p152-posterior-detail" x="731" y="359" text-anchor="end">${format(values.trueAlarmCount,2)} / ${format(values.alarmCount,2)} alarms</text></svg><div class="p152-status"><strong>${format(values.posteriorPercent,3)}% of alarms indicate a meteor</strong><span>True alarms ${format(values.trueAlarmCount,1)} · false alarms ${format(values.falseAlarmCount,1)} per ${BASIS} nights</span></div></div>`;
}
function metricsMarkup(){const values=bayesValues();return `<div class="p152-metrics" aria-live="polite"><div><span>All alarms</span><strong>${format(values.alarmCount,2)} / ${BASIS}</strong><small>true plus false alarms</small></div><div><span>True meteor alarms</span><strong>${format(values.trueAlarmCount,2)}</strong><small>prior × sensitivity × ${BASIS}</small></div><div><span>Posterior probability</span><strong>${format(values.posteriorPercent,4)}%</strong><small>P(meteor | alarm)</small></div></div>`;}
function dynamicMarkup(){return `<div class="p152-dynamic">${probabilityVisual()}${metricsMarkup()}</div>`;}
function controlsMarkup(){return `<section class="p152-controls" aria-label="Meteor alarm assumptions"><div class="p152-control-grid"><label for="p152-prior"><span>Prior meteor rate<output data-p152-live="prior">${format(state.priorPercent,2)}%</output></span><input id="p152-prior" data-p152-slider="prior" type="range" min="0.1" max="20" step="0.1" value="${state.priorPercent}"/></label><label for="p152-sensitivity"><span>Sensitivity P(alarm | meteor)<output data-p152-live="sensitivity">${format(state.sensitivityPercent,2)}%</output></span><input id="p152-sensitivity" data-p152-slider="sensitivity" type="range" min="50" max="100" step="0.5" value="${state.sensitivityPercent}"/></label><label for="p152-false-positive"><span>False-positive rate P(alarm | no meteor)<output data-p152-live="falsePositive">${format(state.falsePositivePercent,2)}%</output></span><input id="p152-false-positive" data-p152-slider="falsePositive" type="range" min="0" max="20" step="0.1" value="${state.falsePositivePercent}"/></label></div></section>`;}
function feedbackMarkup(){return state.feedback?`<div class="p152-feedback is-${state.feedbackTone}" role="status">${state.feedback}</div>`:"";}
function hintsMarkup(){return state.hintsUsed?`<div class="hint-stack p152-hints" aria-live="polite">${hints.slice(0,state.hintsUsed).map((hint,index)=>`<div class="hint"><strong>Hint ${index+1}.</strong> ${hint}</div>`).join("")}</div>`:"";}
function solutionMarkup(){if(!state.revealed)return"";return `<section class="p152-solution" aria-labelledby="p152-solution-heading"><h3 id="p152-solution-heading" tabindex="-1">An alarm changes the odds, but false alarms dominate</h3><div class="p152-equation">P(M∩A)=P(M)P(A|M)=(0.01)(0.90)=0.009</div><div class="p152-equation">P(not M∩A)=(0.99)(0.05)=0.0495</div><div class="p152-equation">P(A)=0.009+0.0495=0.0585</div><div class="p152-equation is-answer">P(M|A)=0.009/0.0585=0.153846…=${format(challenge.posteriorPercent,6)}%</div><p>In a frequency model of ${BASIS} nights, there are 90 true alarms and 495 false alarms. Thus only 90 of 585 alarms correspond to meteors.</p><p class="p152-limits"><strong>Model boundary.</strong> Nights are treated as independent and the prior, sensitivity and false-positive rate are fixed and known. The siren emits only a binary alarm; evidence strength, changing meteor conditions and calibration uncertainty are omitted. This is a probability model, not an operational warning system.</p></section>`;}
function snapshot(){const values=bayesValues();return JSON.stringify({problem:PROBLEM,provenance:"Original extension created for this project; not present in Professor Povey's Perplexing Problems",basisNights:BASIS,priorMeteorPercent:state.priorPercent,sensitivityPercent:state.sensitivityPercent,falsePositivePercent:state.falsePositivePercent,trueAlarmProbability:Number(values.meteorAndAlarm.toFixed(12)),falseAlarmProbability:Number(values.noMeteorAndAlarm.toFixed(12)),totalAlarmProbability:Number(values.alarmProbability.toFixed(12)),posteriorMeteorGivenAlarmPercent:Number(values.posteriorPercent.toFixed(9)),expectedFrequencies:{meteorNights:Number(values.meteorCount.toFixed(6)),ordinaryNights:Number(values.noMeteorCount.toFixed(6)),trueAlarms:Number(values.trueAlarmCount.toFixed(6)),missedMeteors:Number(values.missedMeteorCount.toFixed(6)),falseAlarms:Number(values.falseAlarmCount.toFixed(6)),correctSilences:Number(values.correctSilenceCount.toFixed(6)),allAlarms:Number(values.alarmCount.toFixed(6))},residuals:{population:Number(values.populationResidual.toFixed(12)),branches:Number(values.branchResidual.toFixed(12)),alarms:Number(values.alarmResidual.toFixed(12))},answer:state.answer||null,committed:state.committed,hintsUsed:state.hintsUsed,solutionRevealed:state.revealed},null,2);}
function render(){return `<main class="book-shell p152-shell">${warning()}<header class="book-header"><div class="book-brand"><strong>Perplexing Problems</strong><span class="eyebrow">Original probability extension</span></div><div class="book-progress">${problemProgress(PROBLEM)}</div>${problemHeaderActions(PROBLEM,resetMarkup)}</header><div class="book-spread p152-spread"><article class="book-page p152-problem-page"><div class="problem-number">Problem 15.2</div><h1 class="book-title p152-title">The Meteor Siren Lies Politely</h1><div class="difficulty" aria-label="Two star difficulty">★★</div><p class="p152-extension-note">Original extension. This chapter and activity were created for this project and do not appear in Professor Povey’s Perplexing Problems.</p><p class="problem-copy">On any night, the prior probability of a meteor is 1.00%. The siren sounds on 90.0% of meteor nights, but also on 5.00% of nights without a meteor.</p><p class="problem-copy">The siren sounds. What is the probability that there really is a meteor?</p><section class="p152-model-card"><strong>Condition on the alarm population</strong><p>An alarm may be true or false. Bayes’ rule compares the frequency of those two routes, not sensitivity alone.</p></section><section class="prediction-box"><div class="eyebrow">Commit before calculating</div><p>Should a 90%-sensitive siren make you 90% confident, or does the rare prior matter more?</p></section></article><section class="book-page book-stage p152-stage" aria-labelledby="p152-stage-title"><div class="p152-stage-heading"><div><span class="eyebrow">Bayes laboratory</span><h2 id="p152-stage-title">Follow every alarm to its source</h2></div><p>Vary the prior, sensitivity and false-positive rate. The tree and frequency mosaic tell the same story in two languages.</p></div>${dynamicMarkup()}${controlsMarkup()}</section><aside class="book-page book-coach p152-coach"><div class="coach-kicker">Solve the fixed challenge</div><p class="coach-question">Given that the siren sounds, what percentage of alarms correspond to a meteor?</p><form class="p152-answer-form" data-p152-answer-form novalidate><label for="p152-answer">P(meteor | alarm)</label><div><input id="p152-answer" type="text" inputmode="decimal" autocomplete="off" value="${escapeAttribute(state.answer)}" placeholder="probability"/><span>%</span><button class="primary-button" type="submit">Check</button></div></form>${feedbackMarkup()}<div class="button-row p152-help-row"><button class="secondary-button" type="button" data-problem-action="p152-hint" ${state.hintsUsed>=hints.length?"disabled":""}>${state.hintsUsed?"Another hint":"Give me a hint"}</button><button class="ghost-button" type="button" data-problem-action="p152-reveal" ${state.revealed?"disabled":""}>${state.revealed?"Solution revealed":"Reveal solution"}</button></div>${hintsMarkup()}${solutionMarkup()}<div class="p152-debug">${debugPanel("Development state",snapshot())}</div></aside></div>${problemNav(PROBLEM)}</main>`;}
function updateDynamicDom(root){const dynamic=root.querySelector(".p152-dynamic");if(dynamic)dynamic.outerHTML=dynamicMarkup();const outputs={prior:`${format(state.priorPercent,2)}%`,sensitivity:`${format(state.sensitivityPercent,2)}%`,falsePositive:`${format(state.falsePositivePercent,2)}%`};Object.entries(outputs).forEach(([key,value])=>root.querySelectorAll(`[data-p152-live="${key}"]`).forEach((node)=>{node.textContent=value;}));const values=bayesValues();root.querySelector("#p152-prior")?.setAttribute("aria-valuetext",`Prior meteor probability ${format(state.priorPercent,2)} percent; posterior after alarm ${format(values.posteriorPercent,3)} percent`);root.querySelector("#p152-sensitivity")?.setAttribute("aria-valuetext",`Alarm sensitivity ${format(state.sensitivityPercent,2)} percent; ${format(values.trueAlarmCount,2)} true alarms per ${BASIS} nights`);root.querySelector("#p152-false-positive")?.setAttribute("aria-valuetext",`False-positive rate ${format(state.falsePositivePercent,2)} percent; ${format(values.falseAlarmCount,2)} false alarms per ${BASIS} nights`);root.querySelectorAll(".state-surface").forEach((surface)=>{surface.textContent=snapshot();});}
function renderAndFocus(rerender,selector){rerender();window.requestAnimationFrame(()=>document.querySelector(selector)?.focus());}
function bind({render:rerender}){const root=document.querySelector(".p152-shell");if(!root)return;root.querySelectorAll("[data-p152-slider]").forEach((slider)=>slider.addEventListener("input",(event)=>{const kind=event.target.dataset.p152Slider;if(kind==="prior")state.priorPercent=clamp(event.target.value,.1,20);if(kind==="sensitivity")state.sensitivityPercent=clamp(event.target.value,50,100);if(kind==="falsePositive")state.falsePositivePercent=clamp(event.target.value,0,20);state.feedback="";state.committed=false;updateDynamicDom(root);}));root.querySelectorAll("[data-problem-action]").forEach((control)=>control.addEventListener("click",()=>{const action=control.dataset.problemAction;if(action==="p152-reset"){state=initialState();renderAndFocus(rerender,"#p152-prior");return;}if(action==="p152-hint")state.hintsUsed=Math.min(hints.length,state.hintsUsed+1);if(action==="p152-reveal")state.revealed=true;rerender();if(action==="p152-reveal")window.requestAnimationFrame(()=>document.querySelector("#p152-solution-heading")?.focus());}));root.querySelector("#p152-answer")?.addEventListener("input",(event)=>{state.answer=event.target.value;state.feedback="";state.feedbackTone="neutral";});root.querySelector("[data-p152-answer-form]")?.addEventListener("submit",(event)=>{event.preventDefault();const raw=event.currentTarget.querySelector("#p152-answer")?.value||"",answer=parsePercent(raw);state.answer=raw.trim();state.committed=false;state.feedbackTone="neutral";if(!Number.isFinite(answer)||answer<0||answer>100){state.feedback="Enter a probability between 0% and 100%; a decimal probability is also accepted.";state.feedbackTone="warn";}else if(Math.abs(answer-challenge.posteriorPercent)<=.02){state.feedback=`Correct. Only ${format(challenge.trueAlarmCount,0)} of ${format(challenge.alarmCount,0)} alarms are true: ${format(challenge.posteriorPercent,4)}%.`;state.feedbackTone="success";state.committed=true;}else if(Math.abs(answer-90)<=.05){state.feedback="That is the sensitivity P(alarm | meteor), not the reversed conditional P(meteor | alarm).";}else if(Math.abs(answer-5)<=.05){state.feedback="That is the false-positive rate. Bayes’ rule must compare false alarms with true alarms.";}else if(Math.abs(answer-1)<=.05){state.feedback="That is the prior before hearing the siren. The alarm raises it, but not to the sensitivity.";}else{state.feedback="Add the true- and false-alarm branches, then ask what fraction of that alarm total came from meteor nights.";}renderAndFocus(rerender,"#p152-answer");});}
window.poveyProblemPages[PROBLEM]={render,bind};
})();
