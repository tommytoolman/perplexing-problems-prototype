(function registerGreatPyramidEstimatePage(){
"use strict";
window.poveyProblemPages=window.poveyProblemPages||{};

const PROBLEM="14.4";
const QUESTION=Object.freeze({baseMetres:230,heightMetres:146,solidPercent:85,densityKgM3:2600,blockMassTonnes:2.5});
const LABOUR=Object.freeze({placementsPerCrewDay:1,daysPerCrewYear:300});
const hints=Object.freeze([
  "Model the outer shape as a square pyramid: Vₑₙᵥ=b²h/3.",
  "The envelope is not all stone in this estimate. Multiply by the assumed solid fraction 0.85.",
  "Mass is stone volume times density. With volume in m³ and density in kg/m³, mass comes out in kg.",
  "One million tonnes is 10⁹ kg. Divide total tonnes by 2.5 tonnes per typical block to get block equivalents.",
  "The labour rung is only an invented scale conversion: at one placement per crew-day and 300 days per crew-year, crew-years=blocks/300."
]);
const resetMarkup='<button class="ghost-button" type="button" data-problem-action="p144-reset">Reset</button>';
const initialState=()=>({baseMetres:QUESTION.baseMetres,heightMetres:QUESTION.heightMetres,solidPercent:QUESTION.solidPercent,densityKgM3:QUESTION.densityKgM3,blockMassTonnes:QUESTION.blockMassTonnes,massAnswer:"",blocksAnswer:"",feedback:"",feedbackTone:"neutral",committed:false,hintsUsed:0,revealed:false});
let state=initialState();

function clamp(value,min,max){return Math.max(min,Math.min(max,Number(value)));}
function format(value,digits=3){if(!Number.isFinite(value))return"—";const rounded=Number(value.toFixed(digits));return Object.is(rounded,-0)?"0":String(rounded);}
function escapeAttribute(value){return String(value).replaceAll("&","&amp;").replaceAll('"',"&quot;").replaceAll("<","&lt;").replaceAll(">","&gt;");}
function estimateValues(baseMetres=state.baseMetres,heightMetres=state.heightMetres,solidPercent=state.solidPercent,densityKgM3=state.densityKgM3,blockMassTonnes=state.blockMassTonnes){
  const envelopeVolumeM3=baseMetres**2*heightMetres/3,solidFraction=solidPercent/100,stoneVolumeM3=envelopeVolumeM3*solidFraction,massKg=stoneVolumeM3*densityKgM3,massTonnes=massKg/1000,massMillionTonnes=massKg/1e9,blockEquivalents=massTonnes/blockMassTonnes,blocksMillion=blockEquivalents/1e6,crewDays=blockEquivalents/LABOUR.placementsPerCrewDay,crewYears=crewDays/LABOUR.daysPerCrewYear;
  return{envelopeVolumeM3,solidFraction,stoneVolumeM3,massKg,massTonnes,massMillionTonnes,blockEquivalents,blocksMillion,crewDays,crewYears,envelopeIdentityResidualM3:envelopeVolumeM3-baseMetres**2*heightMetres/3,massIdentityResidualKg:massKg-stoneVolumeM3*densityKgM3,blockIdentityResidual:massTonnes-blockEquivalents*blockMassTonnes};
}
const challenge=estimateValues(QUESTION.baseMetres,QUESTION.heightMetres,QUESTION.solidPercent,QUESTION.densityKgM3,QUESTION.blockMassTonnes);
function parseNumber(raw){const normalized=String(raw).trim().toLowerCase().replaceAll(",",".");const match=normalized.match(/[-+]?\d*\.?\d+(?:e[-+]?\d+)?/);return match?Number(match[0]):NaN;}
function parseMillionTonnes(raw){let value=parseNumber(raw);if(!Number.isFinite(value))return value;const normalized=String(raw).toLowerCase();if(/kg\b/.test(normalized))value/=1e9;else if(!/million|\bmt\b/.test(normalized)&&/(?:tonne|tonnes|\bt\b)/.test(normalized)&&value>1000)value/=1e6;return value;}
function parseMillionBlocks(raw){let value=parseNumber(raw);if(!Number.isFinite(value))return value;const normalized=String(raw).toLowerCase();if(!/million|\bm\b/.test(normalized)&&/blocks?/.test(normalized)&&value>10000)value/=1e6;return value;}

function pyramidMarkup(){
  const values=estimateValues(),cx=205,groundY=298,baseHalf=108+42*(state.baseMetres-200)/60,backInset=baseHalf*.37,backLift=48,apexY=groundY-(170+75*(state.heightMetres-120)/40),left=cx-baseHalf,right=cx+baseHalf,backLeft=cx-baseHalf+backInset,backRight=cx+baseHalf-backInset,opacity=.25+.52*values.solidFraction;
  const steps=[
    {kicker:"1 · GEOMETRY ENVELOPE",value:`${format(values.envelopeVolumeM3/1e6,4)} million m³`,detail:"b²h/3"},
    {kicker:"2 · ESTIMATED STONE",value:`${format(values.stoneVolumeM3/1e6,4)} million m³`,detail:`× ${format(values.solidFraction,3)} solid fraction`},
    {kicker:"3 · MASS",value:`${format(values.massMillionTonnes,4)} million t`,detail:`× ${format(state.densityKgM3,0)} kg/m³`},
    {kicker:"4 · BLOCK EQUIVALENTS",value:`${format(values.blocksMillion,4)} million`,detail:`÷ ${format(state.blockMassTonnes,2)} t per typical block`},
    {kicker:"5 · ILLUSTRATIVE LABOUR UNIT",value:`${format(values.crewYears,0)} crew-years`,detail:"1 placement/crew-day · 300 days/year"}
  ];
  return `<div class="p144-figure-wrap"><svg class="p144-figure" viewBox="0 0 780 455" role="img" aria-labelledby="p144-figure-title p144-figure-desc"><title id="p144-figure-title">Square-pyramid estimate and assumption ladder</title><desc id="p144-figure-desc">A square pyramid with estimated base side ${format(state.baseMetres,1)} metres and height ${format(state.heightMetres,1)} metres has geometric envelope ${format(values.envelopeVolumeM3,0)} cubic metres. Assuming ${format(state.solidPercent,1)} percent stone at density ${format(state.densityKgM3,0)} kilograms per cubic metre gives ${format(values.massMillionTonnes,4)} million tonnes, equivalent to ${format(values.blocksMillion,4)} million blocks of ${format(state.blockMassTonnes,2)} tonnes each. The labour conversion is illustrative and is not a historical claim.</desc><defs><pattern id="p144-stone-hatch" width="16" height="12" patternUnits="userSpaceOnUse"><path d="M0 8L16 2M-4 12L5 8M12 12L20 8"/></pattern><marker id="p144-arrow" markerWidth="7" markerHeight="7" refX="5.5" refY="3.5" orient="auto"><path d="M0 0 7 3.5 0 7Z"/></marker></defs><text class="p144-panel-title" x="24" y="28">SQUARE-PYRAMID FERMI MODEL · DIMENSIONS ARE ASSUMPTIONS</text><polygon class="p144-face is-left" points="${cx},${format(apexY,2)} ${format(left,2)},${groundY} ${format(backLeft,2)},${groundY-backLift}" opacity="${format(opacity,3)}"/><polygon class="p144-face is-right" points="${cx},${format(apexY,2)} ${format(right,2)},${groundY} ${format(backRight,2)},${groundY-backLift}" opacity="${format(opacity+.08,3)}"/><polygon class="p144-face is-front" points="${cx},${format(apexY,2)} ${format(left,2)},${groundY} ${format(right,2)},${groundY}" opacity="${format(opacity+.12,3)}"/><polygon class="p144-hatch" points="${cx},${format(apexY,2)} ${format(left,2)},${groundY} ${format(right,2)},${groundY}"/><polyline class="p144-base-back" points="${format(left,2)},${groundY} ${format(backLeft,2)},${groundY-backLift} ${format(backRight,2)},${groundY-backLift} ${format(right,2)},${groundY}"/><line class="p144-height" x1="${cx}" y1="${format(apexY,2)}" x2="${cx}" y2="${groundY}"/><text class="p144-dimension is-height" x="${cx+10}" y="${format((apexY+groundY)/2,2)}">h=${format(state.heightMetres,1)} m</text><line class="p144-dimension-line" x1="${format(left,2)}" y1="326" x2="${format(right,2)}" y2="326"/><line class="p144-dimension-cap" x1="${format(left,2)}" y1="319" x2="${format(left,2)}" y2="333"/><line class="p144-dimension-cap" x1="${format(right,2)}" y1="319" x2="${format(right,2)}" y2="333"/><text class="p144-dimension" x="${cx}" y="345" text-anchor="middle">base side b=${format(state.baseMetres,1)} m</text><text class="p144-solid-label" x="${cx}" y="378" text-anchor="middle">estimated stone fraction ${format(state.solidPercent,1)}%</text><text class="p144-estimate-note" x="${cx}" y="403" text-anchor="middle">schematic geometry · not a measured survey</text><line class="p144-divider" x1="405" y1="48" x2="405" y2="425"/>${steps.map((step,index)=>{const top=58+index*70;return `<g class="p144-step"><rect x="430" y="${top}" width="318" height="55" rx="10"/><text class="p144-step-kicker" x="446" y="${top+18}">${step.kicker}</text><text class="p144-step-value" x="446" y="${top+39}">${step.value}</text><text class="p144-step-detail" x="731" y="${top+39}" text-anchor="end">${step.detail}</text></g>${index<steps.length-1?`<line class="p144-step-arrow" x1="589" y1="${top+56}" x2="589" y2="${top+68}" marker-end="url(#p144-arrow)"/>`:""}`;}).join("")}</svg><div class="p144-status"><strong>${format(values.massMillionTonnes,3)} million tonnes · ${format(values.blocksMillion,3)} million block equivalents</strong><span>Every rung inherits the assumptions above it.</span></div></div>`;
}
function metricsMarkup(){const values=estimateValues();return `<div class="p144-metrics" aria-live="polite"><div><span>Estimated stone volume</span><strong>${format(values.stoneVolumeM3/1e6,4)} million m³</strong><small>envelope × solid fraction</small></div><div><span>Estimated mass</span><strong>${format(values.massMillionTonnes,4)} million t</strong><small>stone volume × density</small></div><div><span>Typical-block equivalents</span><strong>${format(values.blocksMillion,4)} million</strong><small>not an archaeological block count</small></div></div>`;}
function dynamicMarkup(){return `<div class="p144-dynamic">${pyramidMarkup()}${metricsMarkup()}</div>`;}
function controlsMarkup(){return `<section class="p144-controls" aria-label="Pyramid estimate assumptions"><div class="p144-control-grid"><label for="p144-base"><span>Base side b<output data-p144-live="base">${format(state.baseMetres,1)} m</output></span><input id="p144-base" data-p144-slider="base" type="range" min="200" max="260" step="1" value="${state.baseMetres}"/></label><label for="p144-height"><span>Height h<output data-p144-live="height">${format(state.heightMetres,1)} m</output></span><input id="p144-height" data-p144-slider="height" type="range" min="120" max="160" step="1" value="${state.heightMetres}"/></label><label for="p144-solid"><span>Estimated solid fraction<output data-p144-live="solid">${format(state.solidPercent,1)}%</output></span><input id="p144-solid" data-p144-slider="solid" type="range" min="60" max="100" step="1" value="${state.solidPercent}"/></label><label for="p144-density"><span>Stone density<output data-p144-live="density">${format(state.densityKgM3,0)} kg/m³</output></span><input id="p144-density" data-p144-slider="density" type="range" min="2200" max="3000" step="25" value="${state.densityKgM3}"/></label><label for="p144-block"><span>Typical block mass<output data-p144-live="block">${format(state.blockMassTonnes,2)} t</output></span><input id="p144-block" data-p144-slider="block" type="range" min="1" max="5" step="0.1" value="${state.blockMassTonnes}"/></label></div></section>`;}
function feedbackMarkup(){return state.feedback?`<div class="p144-feedback is-${state.feedbackTone}" role="status">${state.feedback}</div>`:"";}
function hintsMarkup(){return state.hintsUsed?`<div class="hint-stack p144-hints" aria-live="polite">${hints.slice(0,state.hintsUsed).map((hint,index)=>`<div class="hint"><strong>Hint ${index+1}.</strong> ${hint}</div>`).join("")}</div>`:"";}
function solutionMarkup(){if(!state.revealed)return"";return `<section class="p144-solution" aria-labelledby="p144-solution-heading"><h3 id="p144-solution-heading" tabindex="-1">Climb the assumption ladder</h3><div class="p144-equation">Vₑₙᵥ=(230 m)²(146 m)/3=${format(challenge.envelopeVolumeM3,3)} m³</div><div class="p144-equation">Vstone=0.85Vₑₙᵥ=${format(challenge.stoneVolumeM3,3)} m³</div><div class="p144-equation">M=Vstone(2600 kg/m³)=${format(challenge.massKg,0)} kg=${format(challenge.massMillionTonnes,6)} million t</div><div class="p144-equation is-answer">N=M/(2.5 t)=${format(challenge.blockEquivalents,0)} typical-block equivalents ≈${format(challenge.blocksMillion,3)} million</div><p>Under these rounded assumptions, the estimated stone mass is about ${format(challenge.massMillionTonnes,2)} million tonnes, or ${format(challenge.blocksMillion,2)} million typical-block equivalents.</p><p class="p144-limits"><strong>Estimate boundaries.</strong> The pyramid is treated as a perfect square pyramid. Dimensions, solid fraction, uniform density and “typical block” mass are modelling inputs, not a survey or block inventory. Internal spaces and missing casing are represented only through the solid fraction. The optional labour rung assumes one block placement per crew-day and 300 crew-days per crew-year; it excludes quarrying, dressing, transport, ramps, organisation and variation in block size, so it is not an estimate of workforce or construction time. The historical name in the title is not used to assert any quotation, calculation or event involving Napoleon. Since m²×m=m³ and kg/m³×m³=kg, the SI units close exactly; 10⁹ kg=one million tonnes.</p></section>`;}
function snapshot(){const values=estimateValues();return JSON.stringify({problem:PROBLEM,provenance:"independently reconstructed from title and difficulty only; no historical anecdote asserted",model:"square-pyramid Fermi estimate with explicit solid fraction and illustrative labour conversion",baseSideMetres:state.baseMetres,heightMetres:state.heightMetres,estimatedSolidPercent:state.solidPercent,stoneDensityKilogramsPerCubicMetre:state.densityKgM3,typicalBlockMassTonnes:state.blockMassTonnes,envelopeVolumeCubicMetres:Number(values.envelopeVolumeM3.toFixed(9)),estimatedStoneVolumeCubicMetres:Number(values.stoneVolumeM3.toFixed(9)),estimatedMassKilograms:Number(values.massKg.toFixed(6)),estimatedMassMillionTonnes:Number(values.massMillionTonnes.toFixed(9)),typicalBlockEquivalents:Number(values.blockEquivalents.toFixed(6)),typicalBlockEquivalentsMillions:Number(values.blocksMillion.toFixed(9)),illustrativeCrewDays:Number(values.crewDays.toFixed(6)),illustrativeCrewYearsAt300Days:Number(values.crewYears.toFixed(6)),residuals:{geometryCubicMetres:Number(values.envelopeIdentityResidualM3.toFixed(12)),massKilograms:Number(values.massIdentityResidualKg.toFixed(9)),blocksTonnes:Number(values.blockIdentityResidual.toFixed(9))},massAnswerMillionTonnes:state.massAnswer||null,blocksAnswerMillions:state.blocksAnswer||null,committed:state.committed,hintsUsed:state.hintsUsed,solutionRevealed:state.revealed},null,2);}
function render(){return `<main class="book-shell p144-shell">${warning()}<header class="book-header"><div class="book-brand"><strong>Perplexing Problems</strong><span class="eyebrow">Interactive Fermi geometry</span></div><div class="book-progress">${problemProgress(PROBLEM)}</div>${problemHeaderActions(PROBLEM,resetMarkup)}</header><div class="book-spread p144-spread"><article class="book-page p144-problem-page"><div class="problem-number">Problem 14.4</div><h1 class="book-title p144-title">Napoleon Bonaparte and the Great Pyramid</h1><div class="difficulty" aria-label="One star difficulty">★</div><p class="p144-reconstruction-note"><strong>Reconstructed activity.</strong> The available source preserves this problem’s title and difficulty only. This independently written estimate is not the book’s wording or solution, and it asserts no historical Napoleon anecdote.</p><p class="problem-copy">Approximate a large square pyramid with base side 230 m and height 146 m. Assume 85% of its geometric envelope is stone of density 2600 kg/m³, with a typical block mass of 2.50 tonnes.</p><p class="problem-copy">Estimate the stone mass in millions of tonnes and the number of typical-block equivalents in millions.</p><section class="p144-model-card"><strong>An estimate, not an inventory</strong><p>Each rounded input is visible and adjustable. “Typical blocks” and labour units are scale comparisons, not archaeological or historical counts.</p></section><section class="prediction-box"><div class="eyebrow">Order of magnitude first</div><p>Will the result be thousands, millions or billions of tonnes?</p></section></article><section class="book-page book-stage p144-stage" aria-labelledby="p144-stage-title"><div class="p144-stage-heading"><div><span class="eyebrow">Assumption-ladder laboratory</span><h2 id="p144-stage-title">Geometry into human scale</h2></div><p>Vary dimensions, solid fraction, density and typical block mass. Each rung updates without hiding its inherited assumptions.</p></div>${dynamicMarkup()}${controlsMarkup()}</section><aside class="book-page book-coach p144-coach"><div class="coach-kicker">Solve the fixed estimate</div><p class="coach-question">What mass and typical-block scale follow from the stated assumptions?</p><form class="p144-answer-form" data-p144-answer-form novalidate><label for="p144-mass-answer">Estimated stone mass</label><div><input id="p144-mass-answer" type="text" inputmode="decimal" autocomplete="off" value="${escapeAttribute(state.massAnswer)}" placeholder="mass"/><span>million t</span></div><label for="p144-blocks-answer">Typical-block equivalents</label><div><input id="p144-blocks-answer" type="text" inputmode="decimal" autocomplete="off" value="${escapeAttribute(state.blocksAnswer)}" placeholder="blocks"/><span>million</span></div><button class="primary-button p144-check" type="submit">Check both</button></form>${feedbackMarkup()}<div class="button-row p144-help-row"><button class="secondary-button" type="button" data-problem-action="p144-hint" ${state.hintsUsed>=hints.length?"disabled":""}>${state.hintsUsed?"Another hint":"Give me a hint"}</button><button class="ghost-button" type="button" data-problem-action="p144-reveal" ${state.revealed?"disabled":""}>${state.revealed?"Solution revealed":"Reveal solution"}</button></div>${hintsMarkup()}${solutionMarkup()}<div class="p144-debug">${debugPanel("Development state",snapshot())}</div></aside></div>${problemNav(PROBLEM)}</main>`;}
function updateDynamicDom(root){const dynamic=root.querySelector(".p144-dynamic");if(dynamic)dynamic.outerHTML=dynamicMarkup();const outputs={base:`${format(state.baseMetres,1)} m`,height:`${format(state.heightMetres,1)} m`,solid:`${format(state.solidPercent,1)}%`,density:`${format(state.densityKgM3,0)} kg/m³`,block:`${format(state.blockMassTonnes,2)} t`};Object.entries(outputs).forEach(([key,value])=>root.querySelectorAll(`[data-p144-live="${key}"]`).forEach((node)=>{node.textContent=value;}));const values=estimateValues();root.querySelector("#p144-base")?.setAttribute("aria-valuetext",`Base side ${format(state.baseMetres,1)} metres; envelope volume ${format(values.envelopeVolumeM3/1e6,3)} million cubic metres`);root.querySelector("#p144-height")?.setAttribute("aria-valuetext",`Height ${format(state.heightMetres,1)} metres; envelope volume ${format(values.envelopeVolumeM3/1e6,3)} million cubic metres`);root.querySelector("#p144-solid")?.setAttribute("aria-valuetext",`Estimated solid fraction ${format(state.solidPercent,1)} percent; stone volume ${format(values.stoneVolumeM3/1e6,3)} million cubic metres`);root.querySelector("#p144-density")?.setAttribute("aria-valuetext",`Stone density ${format(state.densityKgM3,0)} kilograms per cubic metre; mass ${format(values.massMillionTonnes,3)} million tonnes`);root.querySelector("#p144-block")?.setAttribute("aria-valuetext",`Typical block mass ${format(state.blockMassTonnes,2)} tonnes; ${format(values.blocksMillion,3)} million block equivalents`);root.querySelectorAll(".state-surface").forEach((surface)=>{surface.textContent=snapshot();});}
function renderAndFocus(rerender,selector){rerender();window.requestAnimationFrame(()=>document.querySelector(selector)?.focus());}
function bind({render:rerender}){const root=document.querySelector(".p144-shell");if(!root)return;root.querySelectorAll("[data-p144-slider]").forEach((slider)=>slider.addEventListener("input",(event)=>{const kind=event.target.dataset.p144Slider;if(kind==="base")state.baseMetres=clamp(event.target.value,200,260);if(kind==="height")state.heightMetres=clamp(event.target.value,120,160);if(kind==="solid")state.solidPercent=clamp(event.target.value,60,100);if(kind==="density")state.densityKgM3=clamp(event.target.value,2200,3000);if(kind==="block")state.blockMassTonnes=clamp(event.target.value,1,5);state.feedback="";state.committed=false;updateDynamicDom(root);}));root.querySelectorAll("[data-problem-action]").forEach((control)=>control.addEventListener("click",()=>{const action=control.dataset.problemAction;if(action==="p144-reset"){state=initialState();renderAndFocus(rerender,"#p144-base");return;}if(action==="p144-hint")state.hintsUsed=Math.min(hints.length,state.hintsUsed+1);if(action==="p144-reveal")state.revealed=true;rerender();if(action==="p144-reveal")window.requestAnimationFrame(()=>document.querySelector("#p144-solution-heading")?.focus());}));root.querySelector("#p144-mass-answer")?.addEventListener("input",(event)=>{state.massAnswer=event.target.value;state.feedback="";state.feedbackTone="neutral";});root.querySelector("#p144-blocks-answer")?.addEventListener("input",(event)=>{state.blocksAnswer=event.target.value;state.feedback="";state.feedbackTone="neutral";});root.querySelector("[data-p144-answer-form]")?.addEventListener("submit",(event)=>{event.preventDefault();const massRaw=event.currentTarget.querySelector("#p144-mass-answer")?.value||"",blocksRaw=event.currentTarget.querySelector("#p144-blocks-answer")?.value||"",mass=parseMillionTonnes(massRaw),blocks=parseMillionBlocks(blocksRaw);state.massAnswer=massRaw.trim();state.blocksAnswer=blocksRaw.trim();state.committed=false;state.feedbackTone="neutral";if(!Number.isFinite(mass)||!Number.isFinite(blocks)||mass<0||blocks<0){state.feedback="Enter two non-negative estimates. Mass is in million tonnes and block equivalents in millions.";state.feedbackTone="warn";}else{const massCorrect=Math.abs(mass-challenge.massMillionTonnes)<=.03,blocksCorrect=Math.abs(blocks-challenge.blocksMillion)<=.02;if(massCorrect&&blocksCorrect){state.feedback=`Correct for this model: about ${format(challenge.massMillionTonnes,2)} million tonnes and ${format(challenge.blocksMillion,2)} million block equivalents.`;state.feedbackTone="success";state.committed=true;}else if(massCorrect){state.feedback="The mass estimate is right. Convert it to tonnes, then divide by 2.50 tonnes per typical block.";}else if(blocksCorrect){state.feedback="The block-equivalent estimate is right. Recheck the 1/3 pyramid factor, solid fraction and kg-to-tonnes conversion.";}else if(Math.abs(mass-estimateValues(230,146,100,2600,2.5).massMillionTonnes)<=.03){state.feedback="That treats the entire geometric envelope as stone. Apply the stated 85% solid fraction.";}else{state.feedback="Move rung by rung: pyramid envelope, solid fraction, density, then typical block mass.";}}renderAndFocus(rerender,"#p144-mass-answer");});}
window.poveyProblemPages[PROBLEM]={render,bind};
})();
