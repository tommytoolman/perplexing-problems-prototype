(function registerEuclidsShrinkingRectanglePage(){
"use strict";
window.poveyProblemPages=window.poveyProblemPages||{};

const PROBLEM="16.4";
const QUESTION=Object.freeze({sideA:1071,sideB:462});
const hints=Object.freeze([
  "Divide the larger side by the smaller: 1071=2×462+147.",
  "Removing whole 462×462 squares leaves a 462×147 rectangle, and the common tile size is unchanged.",
  "Continue: 462=3×147+21.",
  "The positive remainders strictly decrease, so the process must eventually reach remainder zero.",
  "Finally 147=7×21+0, so the last non-zero remainder—and the gcd—is 21."
]);
const resetMarkup='<button class="ghost-button" type="button" data-problem-action="p164-reset">Reset</button>';
const initialState=()=>({sideA:QUESTION.sideA,sideB:QUESTION.sideB,stepIndex:0,answer:"",feedback:"",feedbackTone:"neutral",committed:false,hintsUsed:0,revealed:false});
let state=initialState();

function clampInteger(value,min,max){return Math.max(min,Math.min(max,Math.round(Number(value))));}
function formatInteger(value){return Number(value).toLocaleString("en-GB");}
function escapeAttribute(value){return String(value).replaceAll("&","&amp;").replaceAll('"',"&quot;").replaceAll("<","&lt;").replaceAll(">","&gt;");}
function gcd(a,b){let x=Math.abs(Math.round(a)),y=Math.abs(Math.round(b));while(y!==0){const remainder=x%y;x=y;y=remainder;}return x;}
function euclidTrace(sideA=state.sideA,sideB=state.sideB){let x=Math.max(clampInteger(sideA,1,1500),clampInteger(sideB,1,1500)),y=Math.min(clampInteger(sideA,1,1500),clampInteger(sideB,1,1500));const originalGcd=gcd(x,y),steps=[];while(y>0){const quotient=Math.floor(x/y),remainder=x%y,nextGcd=gcd(y,remainder);steps.push({x,y,quotient,remainder,gcdBefore:gcd(x,y),gcdAfter:nextGcd,invariantResidual:gcd(x,y)-nextGcd,strictDescent:remainder===0||remainder<y});x=y;y=remainder;}return{sideA:clampInteger(sideA,1,1500),sideB:clampInteger(sideB,1,1500),larger:Math.max(sideA,sideB),smaller:Math.min(sideA,sideB),gcd:originalGcd,steps,terminal:x};}
const challenge=euclidTrace(QUESTION.sideA,QUESTION.sideB);
function parseInteger(raw){const normalized=String(raw).trim().replace(/[\s,_]/g,"");const match=normalized.match(/[-+]?\d+(?:\.0+)?/);return match?Number(match[0]):NaN;}

function algorithmVisual(){
  const trace=euclidTrace(),index=Math.min(state.stepIndex,trace.steps.length-1),step=trace.steps[index],maxWidth=360,maxHeight=248,scale=Math.min(maxWidth/step.x,maxHeight/step.y),rectWidth=step.x*scale,rectHeight=step.y*scale,originX=31,originY=76+(maxHeight-rectHeight)/2,squareSize=step.y*scale,tiledWidth=step.quotient*squareSize,remainderWidth=step.remainder*scale,rowHeight=Math.min(44,245/trace.steps.length),traceTop=66;
  return `<div class="p164-visual-wrap"><svg class="p164-visual" viewBox="0 0 790 430" role="img" aria-labelledby="p164-visual-title p164-visual-desc"><title id="p164-visual-title">Euclidean algorithm as maximal square tiling</title><desc id="p164-visual-desc">Step ${index+1} of ${trace.steps.length} divides ${step.x} by ${step.y}: quotient ${step.quotient}, remainder ${step.remainder}. The rectangle is tiled by ${step.quotient} maximal squares with a remainder rectangle of width ${step.remainder}. The gcd remains ${trace.gcd}; the complete algorithm terminates after ${trace.steps.length} divisions.</desc><defs><pattern id="p164-square-pattern" width="${Math.max(squareSize,.01)}" height="${Math.max(squareSize,.01)}" patternUnits="userSpaceOnUse"><rect width="100%" height="100%"/><path d="M0 0H${Math.max(squareSize,.01)}V${Math.max(squareSize,.01)}H0Z"/></pattern></defs><text class="p164-panel-title" x="24" y="26">MAXIMAL-SQUARE TILING · DIVISION ${index+1}/${trace.steps.length}</text><rect class="p164-rectangle" x="${originX}" y="${originY}" width="${rectWidth}" height="${rectHeight}" rx="3"/><rect class="p164-square-zone" x="${originX}" y="${originY}" width="${tiledWidth}" height="${rectHeight}" fill="url(#p164-square-pattern)"/>${step.remainder>0?`<rect class="p164-remainder" x="${originX+tiledWidth}" y="${originY}" width="${remainderWidth}" height="${rectHeight}"/><text class="p164-remainder-label" x="${originX+tiledWidth+remainderWidth/2}" y="${originY+rectHeight/2}" text-anchor="middle">r=${step.remainder}</text>`:""}<rect class="p164-rectangle-outline" x="${originX}" y="${originY}" width="${rectWidth}" height="${rectHeight}"/><text class="p164-dimension" x="${originX+rectWidth/2}" y="${originY+rectHeight+24}" text-anchor="middle">long side ${formatInteger(step.x)}</text><text class="p164-dimension" x="${originX-12}" y="${originY+rectHeight/2}" text-anchor="middle" transform="rotate(-90 ${originX-12} ${originY+rectHeight/2})">short side ${formatInteger(step.y)}</text><text class="p164-square-count" x="${originX+tiledWidth/2}" y="${originY+rectHeight/2-13}" text-anchor="middle">${step.quotient} maximal square${step.quotient===1?"":"s"}</text><text class="p164-square-size" x="${originX+tiledWidth/2}" y="${originY+rectHeight/2+10}" text-anchor="middle">${formatInteger(step.y)} × ${formatInteger(step.y)}</text><rect class="p164-equation-box" x="31" y="350" width="360" height="50" rx="10"/><text class="p164-equation" x="211" y="381" text-anchor="middle">${formatInteger(step.x)} = ${step.quotient}×${formatInteger(step.y)} + ${formatInteger(step.remainder)}</text><line class="p164-divider" x1="414" y1="45" x2="414" y2="404"/><text class="p164-panel-title" x="438" y="51">ALGORITHM TRACE · gcd STAYS ${formatInteger(trace.gcd)}</text>${trace.steps.map((item,stepIndex)=>{const top=traceTop+stepIndex*rowHeight,active=stepIndex===index;return`<g class="p164-trace-row ${active?"is-active":""} ${item.remainder===0?"is-terminal":""}"><rect x="438" y="${top}" width="326" height="${Math.max(rowHeight-5,23)}" rx="8"/><text class="p164-trace-number" x="452" y="${top+Math.min(17,rowHeight*.52)}">${stepIndex+1}</text><text class="p164-trace-equation" x="480" y="${top+Math.min(17,rowHeight*.52)}">${formatInteger(item.x)}=${item.quotient}×${formatInteger(item.y)}+${formatInteger(item.remainder)}</text><text class="p164-trace-gcd" x="748" y="${top+Math.min(17,rowHeight*.52)}" text-anchor="end">gcd ${formatInteger(item.gcdBefore)}</text></g>`;}).join("")}<rect class="p164-invariant-box" x="438" y="330" width="326" height="70" rx="11"/><text class="p164-invariant-kicker" x="454" y="352">INVARIANT AND DESCENT</text><text class="p164-invariant-value" x="454" y="375">gcd(${step.x},${step.y}) = gcd(${step.y},${step.remainder}) = ${trace.gcd}</text><text class="p164-invariant-detail" x="454" y="392">${step.remainder===0?"remainder 0 → terminate":"0 < remainder → next short side is strictly smaller"}</text></svg><div class="p164-status"><strong>gcd remains ${formatInteger(trace.gcd)}</strong><span>${step.remainder===0?`Remainder 0: terminate after ${trace.steps.length} divisions`:`Next rectangle ${formatInteger(step.y)}×${formatInteger(step.remainder)}`}</span></div></div>`;
}
function metricsMarkup(){const trace=euclidTrace(),step=trace.steps[Math.min(state.stepIndex,trace.steps.length-1)];return `<div class="p164-metrics" aria-live="polite"><div><span>Invariant gcd</span><strong>${formatInteger(trace.gcd)}</strong><small>same before and after every cut</small></div><div><span>Current quotient</span><strong>${formatInteger(step.quotient)} squares</strong><small>each ${formatInteger(step.y)}×${formatInteger(step.y)}</small></div><div><span>Current remainder</span><strong>${formatInteger(step.remainder)}</strong><small>${step.remainder===0?"algorithm terminates":`${step.remainder} < ${step.y}`}</small></div></div>`;}
function dynamicMarkup(){return `<div class="p164-dynamic">${algorithmVisual()}${metricsMarkup()}</div>`;}
function controlsMarkup(){const trace=euclidTrace();return `<section class="p164-controls" aria-label="Euclidean algorithm controls"><div class="p164-control-grid"><label for="p164-a"><span>Rectangle side a<output data-p164-live="a">${formatInteger(state.sideA)}</output></span><input id="p164-a" data-p164-slider="a" type="range" min="1" max="1500" step="1" value="${state.sideA}"/></label><label for="p164-b"><span>Rectangle side b<output data-p164-live="b">${formatInteger(state.sideB)}</output></span><input id="p164-b" data-p164-slider="b" type="range" min="1" max="1500" step="1" value="${state.sideB}"/></label></div><div class="p164-step-controls"><span>Division <strong data-p164-step>${Math.min(state.stepIndex,trace.steps.length-1)+1}/${trace.steps.length}</strong></span><div class="button-row"><button class="secondary-button" type="button" data-problem-action="p164-previous" ${state.stepIndex<=0?"disabled":""}>Previous division</button><button class="primary-button" type="button" data-problem-action="p164-next" ${state.stepIndex>=trace.steps.length-1?"disabled":""}>Next division</button><button class="ghost-button" type="button" data-problem-action="p164-restart">Restart trace</button><button class="ghost-button" type="button" data-problem-action="p164-challenge">Challenge values</button></div></div></section>`;}
function feedbackMarkup(){return state.feedback?`<div class="p164-feedback is-${state.feedbackTone}" role="status">${state.feedback}</div>`:"";}
function hintsMarkup(){return state.hintsUsed?`<div class="hint-stack p164-hints" aria-live="polite">${hints.slice(0,state.hintsUsed).map((hint,index)=>`<div class="hint"><strong>Hint ${index+1}.</strong> ${hint}</div>`).join("")}</div>`:"";}
function solutionMarkup(){if(!state.revealed)return"";return `<section class="p164-solution" aria-labelledby="p164-solution-heading"><h3 id="p164-solution-heading" tabindex="-1">The last non-zero remainder is the tile size</h3><div class="p164-solution-step">1071=2×462+147</div><div class="p164-solution-step">462=3×147+21</div><div class="p164-solution-step">147=7×21+0</div><div class="p164-solution-step is-answer">gcd(1071,462)=21</div><p>Removing whole squares does not change which square side lengths tile the rectangle exactly: gcd(a,b)=gcd(b,a mod b). Because every non-zero remainder is a smaller positive integer than the divisor, infinite descent is impossible and the algorithm must terminate.</p><p class="p164-limits"><strong>Geometric model.</strong> Side lengths are positive integers and squares are aligned to the rectangle. Each displayed division removes the maximum possible number of equal squares of side equal to the current short side. The remaining rectangle is then rotated conceptually for the next division. Geometry illustrates the invariant; the remainder recurrence proves termination.</p></section>`;}
function snapshot(){const trace=euclidTrace(),index=Math.min(state.stepIndex,trace.steps.length-1),step=trace.steps[index];return JSON.stringify({problem:PROBLEM,provenance:"Original extension created for this project; not present in Professor Povey's Perplexing Problems",model:"Euclidean algorithm visualised by maximal square tiling",inputSides:{a:state.sideA,b:state.sideB},exactGcd:trace.gcd,divisionCount:trace.steps.length,currentDivision:index+1,currentStep:step,allSteps:trace.steps.map(({x,y,quotient,remainder,gcdBefore,gcdAfter})=>({x,y,quotient,remainder,gcdBefore,gcdAfter})),allInvariantsHold:trace.steps.every((item)=>item.invariantResidual===0),allRemaindersDescend:trace.steps.every((item)=>item.strictDescent),terminalRemainder:trace.steps.at(-1).remainder,challengeAnswer:challenge.gcd,answer:state.answer||null,committed:state.committed,hintsUsed:state.hintsUsed,solutionRevealed:state.revealed},null,2);}
function render(){return `<main class="book-shell p164-shell">${warning()}<header class="book-header"><div class="book-brand"><strong>Perplexing Problems</strong><span class="eyebrow">Original algorithm extension</span></div><div class="book-progress">${problemProgress(PROBLEM)}</div>${problemHeaderActions(PROBLEM,resetMarkup)}</header><div class="book-spread p164-spread"><article class="book-page p164-problem-page"><div class="problem-number">Problem 16.4</div><h1 class="book-title p164-title">Euclid’s Shrinking Rectangle</h1><div class="difficulty" aria-label="Two star difficulty">★★</div><p class="p164-extension-note">Original extension. This chapter and activity were created for this project and do not appear in Professor Povey’s Perplexing Problems.</p><p class="problem-copy">A 1071×462 rectangle is to be tiled by the largest possible identical integer-sided squares. Tile off maximal squares, then repeat on the remainder rectangle.</p><p class="problem-copy">What is the greatest common divisor of 1071 and 462, and why must the process terminate?</p><section class="p164-model-card"><strong>One invariant, one descent</strong><p>Replacing (a,b) by (b,a mod b) preserves the common divisors, while the positive remainder becomes strictly smaller.</p></section><section class="prediction-box"><div class="eyebrow">Commit before stepping</div><p>The first cut leaves width 147. Is that already the final tile size?</p></section></article><section class="book-page book-stage p164-stage" aria-labelledby="p164-stage-title"><div class="p164-stage-heading"><div><span class="eyebrow">Euclidean algorithm laboratory</span><h2 id="p164-stage-title">Cut squares; shrink the remainder</h2></div><p>Change both integer sides and step through each maximal-square tiling beside its exact division trace.</p></div>${dynamicMarkup()}${controlsMarkup()}</section><aside class="book-page book-coach p164-coach"><div class="coach-kicker">Solve the fixed challenge</div><p class="coach-question">What is gcd(1071,462)?</p><form class="p164-answer-form" data-p164-answer-form novalidate><label for="p164-answer">Greatest common divisor</label><div><input id="p164-answer" type="text" inputmode="numeric" autocomplete="off" value="${escapeAttribute(state.answer)}" placeholder="gcd"/><button class="primary-button" type="submit">Check</button></div></form>${feedbackMarkup()}<div class="button-row p164-help-row"><button class="secondary-button" type="button" data-problem-action="p164-hint" ${state.hintsUsed>=hints.length?"disabled":""}>${state.hintsUsed?"Another hint":"Give me a hint"}</button><button class="ghost-button" type="button" data-problem-action="p164-reveal" ${state.revealed?"disabled":""}>${state.revealed?"Solution revealed":"Reveal solution"}</button></div>${hintsMarkup()}${solutionMarkup()}<div class="p164-debug">${debugPanel("Development state",snapshot())}</div></aside></div>${problemNav(PROBLEM)}</main>`;}
function updateDynamicDom(root){const dynamic=root.querySelector(".p164-dynamic");if(dynamic)dynamic.outerHTML=dynamicMarkup();const trace=euclidTrace();root.querySelectorAll('[data-p164-live="a"]').forEach((node)=>{node.textContent=formatInteger(state.sideA);});root.querySelectorAll('[data-p164-live="b"]').forEach((node)=>{node.textContent=formatInteger(state.sideB);});root.querySelectorAll("[data-p164-step]").forEach((node)=>{node.textContent=`${Math.min(state.stepIndex,trace.steps.length-1)+1}/${trace.steps.length}`;});const a=root.querySelector("#p164-a"),b=root.querySelector("#p164-b");if(a){a.value=String(state.sideA);a.setAttribute("aria-valuetext",`Side a ${state.sideA}; greatest common divisor ${trace.gcd}`);}if(b){b.value=String(state.sideB);b.setAttribute("aria-valuetext",`Side b ${state.sideB}; greatest common divisor ${trace.gcd}`);}const previous=root.querySelector('[data-problem-action="p164-previous"]'),next=root.querySelector('[data-problem-action="p164-next"]');if(previous)previous.disabled=state.stepIndex<=0;if(next)next.disabled=state.stepIndex>=trace.steps.length-1;root.querySelectorAll(".state-surface").forEach((surface)=>{surface.textContent=snapshot();});}
function renderAndFocus(rerender,selector){rerender();window.requestAnimationFrame(()=>document.querySelector(selector)?.focus());}
function setSides(root,a,b){state.sideA=clampInteger(a,1,1500);state.sideB=clampInteger(b,1,1500);state.stepIndex=0;state.feedback="";state.committed=false;updateDynamicDom(root);}
function bind({render:rerender}){const root=document.querySelector(".p164-shell");if(!root)return;root.querySelectorAll("[data-p164-slider]").forEach((slider)=>slider.addEventListener("input",(event)=>{const kind=event.target.dataset.p164Slider;if(kind==="a")setSides(root,event.target.value,state.sideB);if(kind==="b")setSides(root,state.sideA,event.target.value);}));root.querySelectorAll("[data-problem-action]").forEach((control)=>control.addEventListener("click",()=>{const action=control.dataset.problemAction;if(action==="p164-reset"){state=initialState();renderAndFocus(rerender,"#p164-a");return;}if(action==="p164-previous"){state.stepIndex=Math.max(0,state.stepIndex-1);updateDynamicDom(root);return;}if(action==="p164-next"){state.stepIndex=Math.min(euclidTrace().steps.length-1,state.stepIndex+1);updateDynamicDom(root);return;}if(action==="p164-restart"){state.stepIndex=0;updateDynamicDom(root);root.querySelector('[data-problem-action="p164-next"]')?.focus();return;}if(action==="p164-challenge"){setSides(root,QUESTION.sideA,QUESTION.sideB);root.querySelector("#p164-a")?.focus();return;}if(action==="p164-hint")state.hintsUsed=Math.min(hints.length,state.hintsUsed+1);if(action==="p164-reveal")state.revealed=true;rerender();if(action==="p164-reveal")window.requestAnimationFrame(()=>document.querySelector("#p164-solution-heading")?.focus());}));root.querySelector("#p164-answer")?.addEventListener("input",(event)=>{state.answer=event.target.value;state.feedback="";state.feedbackTone="neutral";});root.querySelector("[data-p164-answer-form]")?.addEventListener("submit",(event)=>{event.preventDefault();const raw=event.currentTarget.querySelector("#p164-answer")?.value||"",answer=parseInteger(raw);state.answer=raw.trim();state.committed=false;state.feedbackTone="neutral";if(!Number.isFinite(answer)||answer<1){state.feedback="Enter a positive integer greatest common divisor.";state.feedbackTone="warn";}else if(answer===challenge.gcd){state.feedback="Correct. The last non-zero remainder is 21, so gcd(1071,462)=21.";state.feedbackTone="success";state.committed=true;state.sideA=QUESTION.sideA;state.sideB=QUESTION.sideB;state.stepIndex=challenge.steps.length-1;}else if(answer===147){state.feedback="That is only the first remainder. Continue the algorithm until the remainder becomes zero.";}else if(answer===3||answer===7){state.feedback="That is a quotient from the trace, not the greatest common divisor.";}else{state.feedback="Continue replacing the pair by (smaller side, remainder) until the last non-zero remainder.";}renderAndFocus(rerender,"#p164-answer");});}
window.poveyProblemPages[PROBLEM]={render,bind};
})();
