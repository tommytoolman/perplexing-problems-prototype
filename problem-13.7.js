(function registerQuantitativePistonPuzzle(){
"use strict";
window.poveyProblemPages=window.poveyProblemPages||{};

const PROBLEM="13.7";
const QUESTION=Object.freeze({inputAreaCm2:12,outputAreaCm2:300,inputForceN:84,inputTravelCm:25});
const hints=Object.freeze([
  "Convert square centimetres to square metres before calculating pressure: 1 cm²=10⁻⁴ m².",
  "Pascal transmission gives the same gauge pressure at both pistons: F₁/A₁=F₂/A₂.",
  "Therefore F₂=F₁(A₂/A₁). The area ratio is the ideal force advantage.",
  "An incompressible liquid preserves displaced volume: A₁x₁=A₂x₂.",
  "Check the result with work: F₁x₁ and F₂x₂ must be equal in this ideal model."
]);
const resetMarkup='<button class="ghost-button" type="button" data-problem-action="p137-reset">Reset</button>';
const initialState=()=>({inputAreaCm2:QUESTION.inputAreaCm2,outputAreaCm2:QUESTION.outputAreaCm2,inputForceN:QUESTION.inputForceN,inputTravelCm:QUESTION.inputTravelCm,forceAnswer:"",travelAnswer:"",feedback:"",feedbackTone:"neutral",committed:false,hintsUsed:0,revealed:false});
let state=initialState();

function clamp(value,min,max){return Math.max(min,Math.min(max,Number(value)));}
function format(value,digits=3){if(!Number.isFinite(value))return"—";const rounded=Number(value.toFixed(digits));return Object.is(rounded,-0)?"0":String(rounded);}
function escapeAttribute(value){return String(value).replaceAll("&","&amp;").replaceAll('"',"&quot;").replaceAll("<","&lt;").replaceAll(">","&gt;");}
function pressValues(inputAreaCm2=state.inputAreaCm2,outputAreaCm2=state.outputAreaCm2,inputForceN=state.inputForceN,inputTravelCm=state.inputTravelCm){
  const inputAreaM2=inputAreaCm2*1e-4,outputAreaM2=outputAreaCm2*1e-4,inputTravelM=inputTravelCm/100,areaRatio=outputAreaCm2/inputAreaCm2,pressurePa=inputForceN/inputAreaM2,outputForceN=pressurePa*outputAreaM2,displacedVolumeM3=inputAreaM2*inputTravelM,outputTravelM=displacedVolumeM3/outputAreaM2,inputWorkJ=inputForceN*inputTravelM,outputWorkJ=outputForceN*outputTravelM;
  return{inputAreaM2,outputAreaM2,inputTravelM,areaRatio,pressurePa,outputForceN,displacedVolumeM3,displacedVolumeCm3:inputAreaCm2*inputTravelCm,outputTravelM,outputTravelCm:outputTravelM*100,inputWorkJ,outputWorkJ,pressureResidualPa:pressurePa-outputForceN/outputAreaM2,volumeResidualM3:displacedVolumeM3-outputAreaM2*outputTravelM,workResidualJ:inputWorkJ-outputWorkJ};
}
const challenge=pressValues(QUESTION.inputAreaCm2,QUESTION.outputAreaCm2,QUESTION.inputForceN,QUESTION.inputTravelCm);
function parseNumber(raw){const normalized=String(raw).trim().toLowerCase().replaceAll(",",".");const match=normalized.match(/[-+]?\d*\.?\d+(?:e[-+]?\d+)?/);return match?Number(match[0]):NaN;}
function parseForce(raw){let value=parseNumber(raw);if(!Number.isFinite(value))return value;const normalized=String(raw).toLowerCase();if(/k\s*n\b/.test(normalized))value*=1000;return value;}
function parseLengthCm(raw){let value=parseNumber(raw);if(!Number.isFinite(value))return value;const normalized=String(raw).toLowerCase();if(/mm\b/.test(normalized))value/=10;else if(/(^|[^c])m\b/.test(normalized))value*=100;return value;}

function pressMarkup(){
  const values=pressValues();
  const smallWidth=60+60*(Math.sqrt(state.inputAreaCm2)-Math.sqrt(2))/(Math.sqrt(50)-Math.sqrt(2));
  const largeWidth=120+105*(Math.sqrt(state.outputAreaCm2)-Math.sqrt(50))/(Math.sqrt(500)-Math.sqrt(50));
  const smallLeft=177-smallWidth/2,largeLeft=570-largeWidth/2;
  const inputArrow=38+42*(state.inputForceN-20)/230,outputArrow=38+42*(values.outputForceN/Math.max(values.outputForceN,state.inputForceN));
  const travelMax=Math.max(state.inputTravelCm,values.outputTravelCm,1),inputTravelPx=66*state.inputTravelCm/travelMax,outputTravelPx=66*values.outputTravelCm/travelMax;
  return `<div class="p137-press-wrap"><svg class="p137-press" viewBox="0 0 780 420" role="img" aria-labelledby="p137-press-title p137-press-desc"><title id="p137-press-title">Ideal hydraulic press with force, pressure, volume and work readings</title><desc id="p137-press-desc">An input piston of area ${format(state.inputAreaCm2,2)} square centimetres receives ${format(state.inputForceN,2)} newtons and moves ${format(state.inputTravelCm,2)} centimetres. Equal pressure in the liquid produces ${format(values.outputForceN,3)} newtons at the ${format(state.outputAreaCm2,2)} square centimetre output piston, which rises ${format(values.outputTravelCm,4)} centimetres. Input and output work are both ${format(values.inputWorkJ,4)} joules.</desc><defs><marker id="p137-arrow-input" markerWidth="8" markerHeight="8" refX="6.5" refY="4" orient="auto"><path d="M0 0 8 4 0 8Z"/></marker><marker id="p137-arrow-output" markerWidth="8" markerHeight="8" refX="6.5" refY="4" orient="auto"><path d="M0 0 8 4 0 8Z"/></marker><marker id="p137-arrow-travel" markerWidth="7" markerHeight="7" refX="5.5" refY="3.5" orient="auto"><path d="M0 0 7 3.5 0 7Z"/></marker></defs><text class="p137-panel-title" x="25" y="29">IDEAL HYDRAULIC PRESS · SAME HYDRAULIC HEIGHT</text><rect class="p137-cylinder" x="${format(smallLeft-10,2)}" y="105" width="${format(smallWidth+20,2)}" height="186" rx="8"/><rect class="p137-cylinder" x="${format(largeLeft-10,2)}" y="105" width="${format(largeWidth+20,2)}" height="186" rx="8"/><path class="p137-manifold" d="M${format(smallLeft,2)} 260H${format(smallLeft+smallWidth,2)}V307H${format(largeLeft,2)}V260H${format(largeLeft+largeWidth,2)}V342H${format(smallLeft,2)}Z"/><rect class="p137-piston is-input" x="${format(smallLeft,2)}" y="153" width="${format(smallWidth,2)}" height="15" rx="4"/><rect class="p137-piston is-output" x="${format(largeLeft,2)}" y="153" width="${format(largeWidth,2)}" height="15" rx="4"/><line class="p137-rod" x1="177" y1="153" x2="177" y2="91"/><line class="p137-rod" x1="570" y1="153" x2="570" y2="91"/><rect class="p137-load" x="${format(largeLeft+largeWidth*.15,2)}" y="63" width="${format(largeWidth*.7,2)}" height="28" rx="5"/><text class="p137-load-label" x="570" y="81" text-anchor="middle">LOAD</text><line class="p137-force-arrow is-input" x1="177" y1="${format(82-inputArrow,2)}" x2="177" y2="82" marker-end="url(#p137-arrow-input)"/><text class="p137-force-label is-input" x="191" y="${format(64-inputArrow/2,2)}">F₁ ${format(state.inputForceN,2)} N</text><line class="p137-force-arrow is-output" x1="570" y1="91" x2="570" y2="${format(91-outputArrow,2)}" marker-end="url(#p137-arrow-output)"/><text class="p137-force-label is-output" x="584" y="${format(80-outputArrow,2)}">F₂ ${format(values.outputForceN,2)} N</text><text class="p137-area-label" x="177" y="186" text-anchor="middle">A₁ ${format(state.inputAreaCm2,2)} cm²</text><text class="p137-area-label" x="570" y="186" text-anchor="middle">A₂ ${format(state.outputAreaCm2,2)} cm²</text><text class="p137-pressure-kicker" x="390" y="277" text-anchor="middle">PASCAL PRESSURE THROUGHOUT LIQUID</text><text class="p137-pressure-value" x="390" y="300" text-anchor="middle">p=${format(values.pressurePa/1000,4)} kPa</text><line class="p137-travel is-input" x1="${format(smallLeft-28,2)}" y1="158" x2="${format(smallLeft-28,2)}" y2="${format(158+inputTravelPx,2)}" marker-end="url(#p137-arrow-travel)"/><text class="p137-travel-label" x="${format(smallLeft-36,2)}" y="${format(176+inputTravelPx,2)}" text-anchor="end">x₁ ${format(state.inputTravelCm,2)} cm down</text><line class="p137-travel is-output" x1="${format(largeLeft-28,2)}" y1="158" x2="${format(largeLeft-28,2)}" y2="${format(158-outputTravelPx,2)}" marker-end="url(#p137-arrow-travel)"/><text class="p137-travel-label" x="${format(largeLeft-36,2)}" y="${format(145-outputTravelPx,2)}" text-anchor="end">x₂ ${format(values.outputTravelCm,4)} cm up</text><line class="p137-divider" x1="25" y1="360" x2="755" y2="360"/><text class="p137-ledger-kicker" x="32" y="382">VOLUME</text><text class="p137-ledger-value" x="104" y="382">A₁x₁=A₂x₂=${format(values.displacedVolumeCm3,3)} cm³</text><text class="p137-ledger-kicker" x="410" y="382">WORK</text><text class="p137-ledger-value is-work" x="470" y="382">F₁x₁=F₂x₂=${format(values.inputWorkJ,4)} J</text><text class="p137-ratio" x="390" y="407" text-anchor="middle">force advantage A₂/A₁ = ${format(values.areaRatio,4)} · travel ratio x₁/x₂ = ${format(values.areaRatio,4)}</text></svg><div class="p137-status"><strong>${format(values.outputForceN,3)} N out · ${format(values.outputTravelCm,4)} cm up</strong><span>More force is exchanged for proportionally less travel; ideal work stays ${format(values.inputWorkJ,4)} J.</span></div></div>`;
}
function metricsMarkup(){const values=pressValues();return `<div class="p137-metrics" aria-live="polite"><div><span>Pascal pressure</span><strong>${format(values.pressurePa/1000,4)} kPa</strong><small>F₁/A₁ = F₂/A₂</small></div><div><span>Output force</span><strong>${format(values.outputForceN,3)} N</strong><small>${format(values.areaRatio,3)}× input force</small></div><div><span>Output travel</span><strong>${format(values.outputTravelCm,4)} cm</strong><small>input travel ÷ ${format(values.areaRatio,3)}</small></div></div>`;}
function dynamicMarkup(){return `<div class="p137-dynamic">${pressMarkup()}${metricsMarkup()}</div>`;}
function controlsMarkup(){return `<section class="p137-controls" aria-label="Hydraulic press controls"><div class="p137-control-grid"><label for="p137-a1"><span>Input piston area A₁<output data-p137-live="a1">${format(state.inputAreaCm2,1)} cm²</output></span><input id="p137-a1" data-p137-slider="a1" type="range" min="2" max="50" step="1" value="${state.inputAreaCm2}"/></label><label for="p137-a2"><span>Output piston area A₂<output data-p137-live="a2">${format(state.outputAreaCm2,1)} cm²</output></span><input id="p137-a2" data-p137-slider="a2" type="range" min="50" max="500" step="5" value="${state.outputAreaCm2}"/></label><label for="p137-force"><span>Input force F₁<output data-p137-live="force">${format(state.inputForceN,1)} N</output></span><input id="p137-force" data-p137-slider="force" type="range" min="20" max="250" step="1" value="${state.inputForceN}"/></label><label for="p137-travel"><span>Input travel x₁<output data-p137-live="travel">${format(state.inputTravelCm,1)} cm</output></span><input id="p137-travel" data-p137-slider="travel" type="range" min="5" max="50" step="1" value="${state.inputTravelCm}"/></label></div></section>`;}
function feedbackMarkup(){return state.feedback?`<div class="p137-feedback is-${state.feedbackTone}" role="status">${state.feedback}</div>`:"";}
function hintsMarkup(){return state.hintsUsed?`<div class="hint-stack p137-hints" aria-live="polite">${hints.slice(0,state.hintsUsed).map((hint,index)=>`<div class="hint"><strong>Hint ${index+1}.</strong> ${hint}</div>`).join("")}</div>`:"";}
function solutionMarkup(){if(!state.revealed)return"";return `<section class="p137-solution" aria-labelledby="p137-solution-heading"><h3 id="p137-solution-heading" tabindex="-1">Force rises as travel falls</h3><div class="p137-equation">p=F₁/A₁=84 N/(12×10⁻⁴ m²)=70,000 Pa</div><div class="p137-equation">F₂=pA₂=(70,000 Pa)(300×10⁻⁴ m²)=2100 N</div><div class="p137-equation">A₁x₁=A₂x₂ ⇒ x₂=(12 cm²)(25 cm)/(300 cm²)=1.00 cm</div><div class="p137-equation is-answer">F₁x₁=(84 N)(0.25 m)=21 J=(2100 N)(0.0100 m)=F₂x₂</div><p>The output piston supplies 2100 N and rises 1.00 cm. Its 25-fold force advantage is exactly matched by 25-fold less travel.</p><p class="p137-limits"><strong>Ideal model.</strong> The liquid is incompressible; pistons and connecting lines are rigid; seals are frictionless; pistons are massless; motion is quasi-static; there is no leakage or dissipative loss. The piston faces are at the same hydraulic height, so hydrostatic head is deliberately excluded. All pressures shown are gauge pressures. Areas are converted using 1 cm²=10⁻⁴ m² and travel using 1 cm=10⁻² m before SI work is calculated.</p></section>`;}
function snapshot(){const values=pressValues();return JSON.stringify({problem:PROBLEM,provenance:"independently reconstructed from title and difficulty only",model:"ideal incompressible frictionless hydraulic press at one hydraulic height",inputAreaSquareCentimetres:state.inputAreaCm2,outputAreaSquareCentimetres:state.outputAreaCm2,inputForceNewtons:state.inputForceN,inputTravelCentimetres:state.inputTravelCm,areaRatio:Number(values.areaRatio.toFixed(9)),gaugePressurePascals:Number(values.pressurePa.toFixed(9)),outputForceNewtons:Number(values.outputForceN.toFixed(9)),displacedVolumeCubicCentimetres:Number(values.displacedVolumeCm3.toFixed(9)),outputTravelCentimetres:Number(values.outputTravelCm.toFixed(9)),inputWorkJoules:Number(values.inputWorkJ.toFixed(9)),outputWorkJoules:Number(values.outputWorkJ.toFixed(9)),residuals:{pressurePascals:Number(values.pressureResidualPa.toFixed(12)),volumeCubicMetres:Number(values.volumeResidualM3.toFixed(15)),workJoules:Number(values.workResidualJ.toFixed(12))},forceAnswer:state.forceAnswer||null,travelAnswer:state.travelAnswer||null,committed:state.committed,hintsUsed:state.hintsUsed,solutionRevealed:state.revealed},null,2);}
function render(){return `<main class="book-shell p137-shell">${warning()}<header class="book-header"><div class="book-brand"><strong>Perplexing Problems</strong><span class="eyebrow">Interactive hydraulics</span></div><div class="book-progress">${problemProgress(PROBLEM)}</div>${problemHeaderActions(PROBLEM,resetMarkup)}</header><div class="book-spread p137-spread"><article class="book-page p137-problem-page"><div class="problem-number">Problem 13.7</div><h1 class="book-title p137-title">A quantitative piston puzzle</h1><div class="difficulty" aria-label="One star difficulty">★</div><p class="p137-reconstruction-note"><strong>Reconstructed activity.</strong> The available source preserves this problem’s title and difficulty only. This independently written hydraulic-press problem is not the book’s wording or solution.</p><p class="problem-copy">An ideal hydraulic press has piston areas A₁=12.0 cm² and A₂=300 cm². A force of 84.0 N pushes the small piston down by 25.0 cm.</p><p class="problem-copy">Find the output force and the distance the large piston rises.</p><section class="p137-model-card"><strong>Two conservation rules</strong><p>Pressure is transmitted through the liquid, and the volume pushed in must equal the volume pushed out.</p></section><section class="prediction-box"><div class="eyebrow">Before moving the sliders</div><p>A larger output area multiplies force. What quantity must shrink by the same ratio?</p></section></article><section class="book-page book-stage p137-stage" aria-labelledby="p137-stage-title"><div class="p137-stage-heading"><div><span class="eyebrow">Hydraulic press laboratory</span><h2 id="p137-stage-title">Trade travel for force</h2></div><p>Change both areas, the applied force and input travel. Pressure, displaced volume and work remain visibly linked.</p></div>${dynamicMarkup()}${controlsMarkup()}</section><aside class="book-page book-coach p137-coach"><div class="coach-kicker">Solve the fixed challenge</div><p class="coach-question">What output force and rise follow from the stated values?</p><form class="p137-answer-form" data-p137-answer-form novalidate><label for="p137-force-answer">Output force</label><div><input id="p137-force-answer" type="text" inputmode="decimal" autocomplete="off" value="${escapeAttribute(state.forceAnswer)}" placeholder="force"/><span>N</span></div><label for="p137-travel-answer">Output rise</label><div><input id="p137-travel-answer" type="text" inputmode="decimal" autocomplete="off" value="${escapeAttribute(state.travelAnswer)}" placeholder="distance"/><span>cm</span></div><button class="primary-button p137-check" type="submit">Check both</button></form>${feedbackMarkup()}<div class="button-row p137-help-row"><button class="secondary-button" type="button" data-problem-action="p137-hint" ${state.hintsUsed>=hints.length?"disabled":""}>${state.hintsUsed?"Another hint":"Give me a hint"}</button><button class="ghost-button" type="button" data-problem-action="p137-reveal" ${state.revealed?"disabled":""}>${state.revealed?"Solution revealed":"Reveal solution"}</button></div>${hintsMarkup()}${solutionMarkup()}<div class="p137-debug">${debugPanel("Development state",snapshot())}</div></aside></div>${problemNav(PROBLEM)}</main>`;}
function updateDynamicDom(root){const dynamic=root.querySelector(".p137-dynamic");if(dynamic)dynamic.outerHTML=dynamicMarkup();const outputs={a1:`${format(state.inputAreaCm2,1)} cm²`,a2:`${format(state.outputAreaCm2,1)} cm²`,force:`${format(state.inputForceN,1)} N`,travel:`${format(state.inputTravelCm,1)} cm`};Object.entries(outputs).forEach(([key,value])=>root.querySelectorAll(`[data-p137-live="${key}"]`).forEach((node)=>{node.textContent=value;}));const values=pressValues();root.querySelector("#p137-a1")?.setAttribute("aria-valuetext",`Input piston area ${format(state.inputAreaCm2,1)} square centimetres; pressure ${format(values.pressurePa/1000,3)} kilopascals`);root.querySelector("#p137-a2")?.setAttribute("aria-valuetext",`Output piston area ${format(state.outputAreaCm2,1)} square centimetres; output force ${format(values.outputForceN,3)} newtons`);root.querySelector("#p137-force")?.setAttribute("aria-valuetext",`Input force ${format(state.inputForceN,1)} newtons; output force ${format(values.outputForceN,3)} newtons`);root.querySelector("#p137-travel")?.setAttribute("aria-valuetext",`Input travel ${format(state.inputTravelCm,1)} centimetres; output travel ${format(values.outputTravelCm,4)} centimetres`);root.querySelectorAll(".state-surface").forEach((surface)=>{surface.textContent=snapshot();});}
function renderAndFocus(rerender,selector){rerender();window.requestAnimationFrame(()=>document.querySelector(selector)?.focus());}
function bind({render:rerender}){const root=document.querySelector(".p137-shell");if(!root)return;root.querySelectorAll("[data-p137-slider]").forEach((slider)=>slider.addEventListener("input",(event)=>{const kind=event.target.dataset.p137Slider;if(kind==="a1")state.inputAreaCm2=clamp(event.target.value,2,50);if(kind==="a2")state.outputAreaCm2=clamp(event.target.value,50,500);if(kind==="force")state.inputForceN=clamp(event.target.value,20,250);if(kind==="travel")state.inputTravelCm=clamp(event.target.value,5,50);state.feedback="";state.committed=false;updateDynamicDom(root);}));root.querySelectorAll("[data-problem-action]").forEach((control)=>control.addEventListener("click",()=>{const action=control.dataset.problemAction;if(action==="p137-reset"){state=initialState();renderAndFocus(rerender,"#p137-a1");return;}if(action==="p137-hint")state.hintsUsed=Math.min(hints.length,state.hintsUsed+1);if(action==="p137-reveal")state.revealed=true;rerender();if(action==="p137-reveal")window.requestAnimationFrame(()=>document.querySelector("#p137-solution-heading")?.focus());}));root.querySelector("#p137-force-answer")?.addEventListener("input",(event)=>{state.forceAnswer=event.target.value;state.feedback="";state.feedbackTone="neutral";});root.querySelector("#p137-travel-answer")?.addEventListener("input",(event)=>{state.travelAnswer=event.target.value;state.feedback="";state.feedbackTone="neutral";});root.querySelector("[data-p137-answer-form]")?.addEventListener("submit",(event)=>{event.preventDefault();const forceRaw=event.currentTarget.querySelector("#p137-force-answer")?.value||"",travelRaw=event.currentTarget.querySelector("#p137-travel-answer")?.value||"",force=parseForce(forceRaw),travel=parseLengthCm(travelRaw);state.forceAnswer=forceRaw.trim();state.travelAnswer=travelRaw.trim();state.committed=false;state.feedbackTone="neutral";if(!Number.isFinite(force)||!Number.isFinite(travel)||force<0||travel<0){state.feedback="Enter a non-negative output force and rise. N or kN, and mm, cm or m, are accepted.";state.feedbackTone="warn";}else{const forceCorrect=Math.abs(force-challenge.outputForceN)<=2,travelCorrect=Math.abs(travel-challenge.outputTravelCm)<=.02;if(forceCorrect&&travelCorrect){state.feedback="Correct. The press supplies 2100 N through 1.00 cm, so both sides transfer 21.0 J.";state.feedbackTone="success";state.committed=true;}else if(forceCorrect){state.feedback="The force is right. Now conserve liquid volume to find the shorter output travel.";}else if(travelCorrect){state.feedback="The travel is right. Now use equal pressure F/A at the two pistons.";}else if(Math.abs(force-3.36)<=.02){state.feedback="The area ratio has been inverted. The larger output area multiplies the force.";}else{state.feedback="Use equal pressure for force, then equal displaced volume for travel. Work provides a final check.";}}renderAndFocus(rerender,"#p137-force-answer");});}
window.poveyProblemPages[PROBLEM]={render,bind};
})();
